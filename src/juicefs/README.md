We create a "Global Filesystem" (i.e., a "globalfs") using

- JuiceFS
- KeyDB
- GCS = google cloud storage

This is NOT literally some filesystem called `globalfs` but
a specific instance of a global filesystem.
